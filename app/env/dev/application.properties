# Development Environment Configuration
# This file contains configuration for the DEV flavor

# API Configuration
api.base.url="https://dev-api.phoenix.com/v1/"
api.key=dev_api_key_12345

# Debug Settings
debug.mode=true

# Network Configuration
network.timeout=30

# Feature Flags (add as needed)
# feature.progress.tracking=true
# feature.workout.templates=true
