# Production Environment Configuration
# This file contains configuration for the PROD flavor
# WARNING: Do NOT commit API keys to version control!
# Consider using local.properties or environment variables for sensitive data

# API Configuration
api.base.url=https://api.phoenix.com/v1/
api.key=YOUR_PRODUCTION_API_KEY_HERE

# Debug Settings
debug.mode=false

# Network Configuration
network.timeout=30

# Feature Flags (add as needed)
# feature.progress.tracking=true
# feature.workout.templates=true
