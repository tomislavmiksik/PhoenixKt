# Staging Environment Configuration
# This file contains configuration for the STAGING flavor

# API Configuration
api.base.url=https://staging-api.phoenix.com/v1/
api.key=staging_api_key_67890

# Debug Settings
debug.mode=false

# Network Configuration
network.timeout=30

# Feature Flags (add as needed)
# feature.progress.tracking=true
# feature.workout.templates=true
